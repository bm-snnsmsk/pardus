1 - sudo nano /etc/hostname
2 - sudo nano /etc/hosts
3 - reboot
----------------------------------

sudo su pico /etc/hostname/
hostname yeni_kullanici_adi
pico /etc/hosts/

// Cihaz yeniden başlatılınca isim değişmiş olacak